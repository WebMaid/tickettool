export enum TicketStatusEnum {
  OPEN = "open",
  PENDING = "pending",
  RESOLVED = "resolved",
  WAITING_FOR_ISSUER = "waiting-for-issuer",
  WAITING_FOR_EXTERNAL = "waiting-for-external",
  NEEDS_ATTENTION = "needs-attention",
  CLOSED = "closed",
}
